using UnityEngine;
using UnityEngine.Events;

public class BuilderUI : UIBase
{
    // public override void Show(UnityAction callback = null)
    // {
    //     // 부모 기본 동작 유지하고 싶으면
    //     base.Show(callback);  // gameObject.SetActive(true) + callback 호출
    //
    //     // 여기부터 BuilderUI 전용 로직
    //     Debug.Log("BuilderUI 전용 Show 로직");
    // }
}
